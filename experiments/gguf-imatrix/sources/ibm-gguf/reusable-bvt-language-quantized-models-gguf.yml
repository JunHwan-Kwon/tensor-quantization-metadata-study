# This workflow runs Build Verification Tests (BVT) on the quantized models
# using llama.cpp utilities.
name: bvt-hf-language-quantized-models-gguf

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      hf_token:
        required: true
    inputs:
      enable_language_jobs:
        type: boolean
        required: false
        default: false
      repo_id:
        type: string
        required: true
      quantization:
        type: string
        required: true
      target_repo_owner:
        type: string
        required: true
      target_repo_name_ext:
        type: string
        required: true
      llamacpp_build_tag:
        type: string
        required: true
        description: "llama.cpp build tag to use (e.g., 'b6569', 'b6808', 'b8100'). Determines which binary tools to use from ./bin subdirectory."
      ext_log:
        type: string
        required: false
        default: '.log.txt'
      ext_response:
        type: string
        required: false
        default: '.response.txt'
      debug:
        type: boolean
        required: true
        default: false
      trace:
        type: boolean
        required: false
        default: false
      verbose:
        type: boolean
        required: false
        default: true
        description: "Enable verbose output for llama-cli (can cause slowdowns with per-token logging)"

# See: https://github.com/ggerganov/llama.cpp/blob/master/examples/server/tests/unit/test_completion.py
env:
  DEBUG: ${{inputs.debug}}
  VERBOSE: ${{inputs.verbose}}
  # FIND_ARGS: "-not -path '*/.*' -print | sed -e 's;[^/]*/;|___;g;s;___|; |;g'"
  EXT_GGUF: .gguf
  # LLAMA_REQUEST_SEED: 647063347
  # LLAMA_REQUEST_PROMPT: "What is the professional answer to \"Why is the sky blue?\""
  LLAMA_SYSTEM_PROMPT: "You are a helpful assistant. Please ensure responses are professional, accurate, and safe."
  LLAMA_REQUEST_PROMPT: "Why is the sky blue according to science?"
  LLAMA_REQUEST_N_PREDICT: 128 # Reduced from 256 to speed up tests
  LLAMA_CLI_TOOL: llama-cli
  LLAMA_CLI_TEMP: 0.8
  LLAMA_COMPLETION_MIN_VERSION: 8742 # llama-completion available from b8742 onwards
  # LLAMA_RUN_BIN: ./bin/llama-run
  # LLAMA_RUN_FLAGS: "-v -n 9"
  LLAMA_RESPONSE_WORDS:  "Rayleigh,scatter,atmosphere"
  LLAMA_SERVER_HOST: localhost
  LLAMA_SERVER_PORT: 8080
  LLAMACPP_DIR: llama.cpp
  MODEL_DOWNLOAD_DIR: models
  QUANTIZED_MODEL_EXISTS: false
  TEST_LLAMA_SERVER: false
  TEST_LLAMA_CLI: true
  TEST_LLAMA_COMPLETION: false
  TEST_LLAMA_RUN: false
  GH_ARTIFACT_RETENTION: 2

  # Dynamically set variables (pre-declared to avoid linter warnings)
  REPO_ORG: ""  # Set in set-github-env step
  REPO_NAME: ""  # Set in set-github-env step
  TARGET_REPO_ID: ""  # Set in set-derivative-env-vars-1 step
  BASE_FNAME_QUANTIZED_GGUF: ""  # Set in set-derivative-env-vars-1 step
  BASE_FNAME_LOG_FILE: ""  # Set in set-derivative-env-vars-1 step
  BASE_FNAME_RESPONSE_FILE: ""  # Set in set-derivative-env-vars-1 step
  LOCAL_MODEL_PATH: ""  # Set in set-derivative-env-vars-2 step
  LOCAL_FNAME_QUANTIZED_GGUF: ""  # Set in set-derivative-env-vars-3 step
  IS_SPLIT_GGUF: "0"  # Set in test-quantized-model-exists: 1 = quantized GGUF was uploaded as shards
  SHARD1_FNAME: ""  # Set in test-quantized-model-exists: first shard filename when IS_SPLIT_GGUF=1

jobs:
  test-quantized-model:
    runs-on: gguf-linux-arm64-32-128GB-1200GB
    timeout-minutes: 60
    if: ${{ inputs.enable_language_jobs == true }}
    env:
      HF_HUB_DISABLE_XET: 1
      HF_HUB_ENABLE_HF_TRANSFER: 0
    steps:
    - uses: actions/checkout@v6
      with:
        sparse-checkout: |
          requirements.txt
          scripts/hf_model_file_exists.py
          scripts/hf_file_download.py
          scripts/test_regex_match_file.py
          scripts/test_regex_match_file_2.py
          bin/${{ inputs.llamacpp_build_tag }}/**
        sparse-checkout-cone-mode: false
        fetch-depth: 1
    - name: detect-platform
      run: |
        OS=$(echo "${{ runner.os }}" | tr '[:upper:]' '[:lower:]' | sed 's/macos/darwin/')
        ARCH=$(echo "${{ runner.arch }}" | tr '[:upper:]' '[:lower:]' | sed 's/x64/x86_64/')
        echo "LLAMA_BIN_DIR=bin/${{ inputs.llamacpp_build_tag }}/${OS}-${ARCH}" >> $GITHUB_ENV
        echo "Detected platform: ${OS}-${ARCH} → LLAMA_BIN_DIR=bin/${{ inputs.llamacpp_build_tag }}/${OS}-${ARCH}"
    - name: confirm-environment
      run: |
        echo "[INFO] github.env:"
        echo "[INFO] >> run_id: '${{ github.run_id }}'"
        echo "[INFO] >> ref: '${{ github.ref }}', ref_name: '${{ github.ref_name }}', ref_type: '${{ github.ref_type }}'"
        echo "[INFO] >> workflow_ref: '${{ github.workflow_ref }}'"
        echo "[INFO] >> event_name: '${{ github.event_name }}'"
        echo "[INFO] >> event.: action: '${{ github.event.action }}'"
        echo "[INFO] >> event.: base_ref: '${{ github.event.base_ref }}'"
        echo "[INFO] >> event.: workflow_run.conclusion: '${{ github.event.workflow_run.conclusion }}'"
        echo "[INFO] >> event.release.: name: '${{ github.event.release.name }}', tag_name: '${{ github.event.release.tag_name }}'"
        echo "[INFO] >> llamacpp_build_tag: '${{ inputs.llamacpp_build_tag }}'"

    - name: Dump GitHub context
      env:
        GH_CONTEXT: ${{ toJson(github) }}
      run: echo "$GH_CONTEXT"

    - name: Dump GitHub inputs
      env:
        GITHUB_INPUTS: ${{ toJson(inputs) }}
      run: echo "$GITHUB_INPUTS"

    # - name: List all environment variables
    #   if: ${{ github.event.inputs.debug }}
    #   run: env | sort

    # Note: at the current time, we cannot use Python versions > 3.11 due to HF and langchain deps.
    # Note: you can verify in a step using: run: python -c "import sys; print(sys.version)"
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    # primarily huggingface_hub
    - name: install-dependencies
      run: |
        python -m pip install -r ./requirements.txt
        pip list

    # List available devices (CPU/GPU) that llama.cpp can utilize
    - name: list-available-devices
      run: |
        echo "=========================================="
        echo "Listing available devices for llama.cpp  "
        echo "=========================================="
        ./$LLAMA_BIN_DIR/llama-completion --list-devices || echo "Note: --list-devices flag may not be available in build ${{ inputs.llamacpp_build_tag }}"
        echo "=========================================="

    # Use this step to set values to the github context (shared across jobs/steps)
    # Note: using $GITHUB_OUTPUT sets values under the current step's namespace
    # whereas using $GITHUB_ENV sets values in the job's underlying environment.
    # Note: for each 'repo_id' we parse out e.g., REPO_ORG=ibm-granite REPO_NAME=granite-3.0-2b-instruct
    - name: set-github-env
      id: set_github_env
      run: |
        echo "REPO_ORG=$(dirname '${{ inputs.repo_id }}')" >> $GITHUB_ENV
        echo "REPO_NAME=$(basename '${{ inputs.repo_id }}')" >> $GITHUB_ENV

    - name: set-derivative-env-vars-1
      run: |
        echo "TARGET_REPO_ID=${{inputs.target_repo_owner}}/${{env.REPO_NAME}}${{inputs.target_repo_name_ext}}" >> $GITHUB_ENV
        echo "BASE_FNAME_QUANTIZED_GGUF=${{ env.REPO_NAME }}-${{inputs.quantization}}${{env.EXT_GGUF}}" >> $GITHUB_ENV
        echo "BASE_FNAME_LOG_FILE=${{env.REPO_NAME}}-${{inputs.quantization}}-${{env.LLAMA_CLI_TOOL}}${{inputs.ext_log}}" >> $GITHUB_ENV
        echo "BASE_FNAME_RESPONSE_FILE=${{env.REPO_NAME}}-${{inputs.quantization}}-${{env.LLAMA_CLI_TOOL}}${{inputs.ext_response}}" >> $GITHUB_ENV

    - name: set-derivative-env-vars-2
      run: |
        echo "LOCAL_MODEL_PATH=${{env.MODEL_DOWNLOAD_DIR}}/${{ env.TARGET_REPO_ID }}" >> $GITHUB_ENV

    - name: set-derivative-env-vars-3
      run: |
        echo "LOCAL_FNAME_QUANTIZED_GGUF=${{env.LOCAL_MODEL_PATH}}/${{env.BASE_FNAME_QUANTIZED_GGUF}}" >> $GITHUB_ENV

    - name: verify-github-env
      run: |
        echo "================== Derivative Environment Variables 1 =================="
        echo "TARGET_REPO_ID='$TARGET_REPO_ID' (${{ env.TARGET_REPO_ID }})"
        echo "BASE_FNAME_QUANTIZED_GGUF='$BASE_FNAME_QUANTIZED_GGUF' (${{ env.BASE_FNAME_QUANTIZED_GGUF }})"
        echo "BASE_FNAME_LOG_FILE='$BASE_FNAME_LOG_FILE' (${{ env.BASE_FNAME_LOG_FILE }})"
        echo "BASE_FNAME_RESPONSE_FILE='$BASE_FNAME_RESPONSE_FILE' (${{ env.BASE_FNAME_RESPONSE_FILE }})"
        echo "================== Derivative Environment Variables 2 =================="
        echo "LOCAL_MODEL_PATH='$LOCAL_MODEL_PATH' (${{ env.LOCAL_MODEL_PATH }})"
        echo "================== Derivative Environment Variables 3 =================="
        echo "LOCAL_FNAME_QUANTIZED_GGUF='$LOCAL_FNAME_QUANTIZED_GGUF' (${{ env.LOCAL_FNAME_QUANTIZED_GGUF }})"

        # Determine if llama-completion should be tested (b8742 and higher)
        LLAMACPP_BUILD_TAG="${{ inputs.llamacpp_build_tag }}"
        if [[ "$LLAMACPP_BUILD_TAG" =~ ^b([0-9]+)$ ]]; then
          VERSION_NUM="${BASH_REMATCH[1]}"
          if [ "$VERSION_NUM" -lt ${{ env.LLAMA_COMPLETION_MIN_VERSION }} ]; then
            echo "TEST_LLAMA_COMPLETION=false" >> $GITHUB_ENV
          fi
        else
          # Non-standard build tag, disable llama-completion test
          echo "TEST_LLAMA_COMPLETION=false" >> $GITHUB_ENV
        fi
        echo "Test llama-completion: $TEST_LLAMA_COMPLETION (build: $LLAMACPP_BUILD_TAG, min version: b${{ env.LLAMA_COMPLETION_MIN_VERSION }})"

    - name: test-quantized-model-exists
      run: |
        # First check for the unsplit (single-file) quantized GGUF.
        # If not found, probe for the first shard (-00001-of-NNNNN.gguf) which indicates
        # the quantized GGUF was uploaded as split files by the quantize workflow.
        UNSPLIT_EXISTS=$(python ./scripts/hf_model_file_exists.py ${{ env.TARGET_REPO_ID }} ${{ env.BASE_FNAME_QUANTIZED_GGUF }} ${{secrets.hf_token}})
        echo "Unsplit file check → '$UNSPLIT_EXISTS'"

        if [[ "$UNSPLIT_EXISTS" == "True" ]]; then
          echo "✅ Unsplit quantized GGUF found: '${{env.BASE_FNAME_QUANTIZED_GGUF}}'"
          echo "QUANTIZED_MODEL_EXISTS=true" >> $GITHUB_ENV
          echo "IS_SPLIT_GGUF=0" >> $GITHUB_ENV
        else
          # Probe for first shard: <basename>-00001-of-0000N.gguf (N = 2..9)
          BASENAME="${{ env.BASE_FNAME_QUANTIZED_GGUF }}"
          BASENAME="${BASENAME%.gguf}"
          SHARD1_FOUND=""
          for N in $(seq -w 2 9); do
            CANDIDATE="${BASENAME}-00001-of-0000${N}.gguf"
            SHARD_EXISTS=$(python ./scripts/hf_model_file_exists.py ${{ env.TARGET_REPO_ID }} "$CANDIDATE" ${{secrets.hf_token}})
            if [[ "$SHARD_EXISTS" == "True" ]]; then
              SHARD1_FOUND="$CANDIDATE"
              echo "✅ Split quantized GGUF detected — first shard: '$SHARD1_FOUND'"
              echo "QUANTIZED_MODEL_EXISTS=true" >> $GITHUB_ENV
              echo "IS_SPLIT_GGUF=1" >> $GITHUB_ENV
              echo "SHARD1_FNAME=$SHARD1_FOUND" >> $GITHUB_ENV
              break
            fi
          done
          if [[ -z "$SHARD1_FOUND" ]]; then
            echo "❌ FAILURE: neither unsplit nor split quantized GGUF found in '${{ env.TARGET_REPO_ID }}'."
            exit 2
          fi
        fi

    - name: download-quantized-gguf-hf-hub-download
      if: env.QUANTIZED_MODEL_EXISTS == 'true' && env.IS_SPLIT_GGUF == '0'
      run: |
        echo "Downloading unsplit quantized GGUF: '${{env.BASE_FNAME_QUANTIZED_GGUF}}'..."
        echo "--------------------"
        python ./scripts/hf_file_download.py ${{ env.MODEL_DOWNLOAD_DIR}} ${{ env.TARGET_REPO_ID }} ${{ env.BASE_FNAME_QUANTIZED_GGUF }} ${{secrets.hf_token}}
        ls -al ${{env.MODEL_DOWNLOAD_DIR}}/${{ env.TARGET_REPO_ID }}/*${{env.EXT_GGUF}}

    - name: download-and-merge-split-quantized-gguf
      if: env.QUANTIZED_MODEL_EXISTS == 'true' && env.IS_SPLIT_GGUF == '1'
      run: |
        # The quantized GGUF was uploaded as shards. Download every shard then merge
        # them back into a single file using llama-gguf-split --merge.
        MERGE_TOOL="./$LLAMA_BIN_DIR/llama-gguf-split"
        if [ ! -f "$MERGE_TOOL" ]; then
          echo "::error::llama-gguf-split not found at $MERGE_TOOL (build tag: ${{ inputs.llamacpp_build_tag }})"
          exit 1
        fi
        chmod +x "$MERGE_TOOL"

        DEST_DIR="${{ env.MODEL_DOWNLOAD_DIR }}/${{ env.TARGET_REPO_ID }}"
        mkdir -p "$DEST_DIR"

        BASENAME="${{ env.BASE_FNAME_QUANTIZED_GGUF }}"
        BASENAME="${BASENAME%.gguf}"

        # Parse total shard count from the first shard filename
        FIRST="${{ env.SHARD1_FNAME }}"
        TOTAL=$(echo "$FIRST" | grep -oP '(?<=of-)\d+(?=\.gguf)')
        echo "Downloading $TOTAL shard(s) for '${{ env.BASE_FNAME_QUANTIZED_GGUF }}'..."

        for SHARD_INDEX in $(seq 1 $((10#$TOTAL))); do
          PADDED=$(printf "%05d" $SHARD_INDEX)
          SHARD_FILE="${BASENAME}-${PADDED}-of-${TOTAL}.gguf"
          echo "Downloading shard: '$SHARD_FILE'"
          python ./scripts/hf_file_download.py "${{ env.MODEL_DOWNLOAD_DIR }}" "${{ env.TARGET_REPO_ID }}" "$SHARD_FILE" ${{secrets.hf_token}}
          SHARD_PATH="$DEST_DIR/$SHARD_FILE"
          if [ ! -f "$SHARD_PATH" ]; then
            echo "::error::Expected shard not found after download: $SHARD_PATH"
            exit 1
          fi
          ls -lh "$SHARD_PATH"
        done

        # Merge all shards into a single quantized GGUF for testing
        FIRST_SHARD="$DEST_DIR/${{ env.SHARD1_FNAME }}"
        MERGED_FILE="$DEST_DIR/${{ env.BASE_FNAME_QUANTIZED_GGUF }}"
        echo "Merging shards → '$MERGED_FILE'"
        "$MERGE_TOOL" --merge "$FIRST_SHARD" "$MERGED_FILE"
        ls -lh "$MERGED_FILE"

        # Remove the individual shards to free runner disk space
        echo "Removing shard files to free disk space..."
        rm -f "$DEST_DIR/${BASENAME}"-*-of-*.gguf
        ls -lh "$DEST_DIR"

    # - name: verify-downloaded-files
    #   if: env.DEBUG == 'true' && runner.os == 'macOS'
    #   run: |
    #     echo downloaded files...
    #     echo "--------------------"
    #     find . -name \*.gguf -type f
    #     echo "--------------------"

    - name: Print Storage & Memory Info (macOS)
      if: inputs.debug == true && runner.os == 'macOS'
      run: |
        df
        echo "--------------------"
        xcodebuild -version
        clang --version
        echo "--------------------"
        top -l 1 | grep -E "^CPU|^PhysMem"
        echo "--------------------"

    - name: Print Storage & Memory Info (Linux)
      if: inputs.debug == true  && runner.os == 'Linux'
      run: |
        df -h
        echo "--------------------"
        gcc --version || lsb_release -a
        echo "--------------------"
        free -h
        echo "--------------------"
        top -b -n 1 | head -n 5
        echo "--------------------"

    # TODO:
    # - name: validate-sha256
    #   if: env.DEBUG == 'true'
    #   run: |
    #     echo "validating sha256..."
    #     echo "--------------------"
    #     # ./bin/llama-gguf-hash --sha256 ~/Downloads/granite-3.0-1b-a400m-instruct-Q4_K_M.gguf > sha256.txt

    # NOTE: Redirecting stderr (2>) to suppress progress indicators and other non-essential output
    # Extended timeout to 60 minutes to accommodate larger models (e.g., 8B models with Q8_0/bf16 quantizations)
    # OPTIMIZATION: Verbose flag is now configurable via inputs.verbose (default: true)
    # Verbose mode causes per-token parsing overhead; disable if tests timeout
    # Added --no-display-prompt and --simple-io to suppress ASCII art and progress indicators
    - name: test-llama-cli
      if: env.TEST_LLAMA_CLI == 'true'
      timeout-minutes: 45
      continue-on-error: true
      run: |
        echo "invoking llama-cli from build tag: ${{ inputs.llamacpp_build_tag }}"
        echo "env.LOCAL_FNAME_QUANTIZED_GGUF=${{env.LOCAL_FNAME_QUANTIZED_GGUF}}"
        echo "VERBOSE mode: ${{env.VERBOSE}}"

        VERBOSE_FLAG=""
        if [[ "${{env.VERBOSE}}" == "true" ]]; then
          VERBOSE_FLAG="--verbose"
        fi

        ./$LLAMA_BIN_DIR/llama-cli \
          -m ./${{env.LOCAL_FNAME_QUANTIZED_GGUF}} \
          -sys '${{env.LLAMA_SYSTEM_PROMPT}}' \
          -p '${{env.LLAMA_REQUEST_PROMPT}}' \
          -n ${{env.LLAMA_REQUEST_N_PREDICT}} \
          --temp ${{env.LLAMA_CLI_TEMP}} \
          $VERBOSE_FLAG \
          --single-turn \
          --no-display-prompt \
          --simple-io \
          --log-file ${{env.BASE_FNAME_LOG_FILE}} 1>${{env.BASE_FNAME_RESPONSE_FILE}}

        EXIT_CODE=$?
        if [ $EXIT_CODE -ne 0 ]; then
          echo "❌ [ERROR] llama-cli failed with exit code $EXIT_CODE:"
          ls -al
          echo "llama-cli: ${{env.BASE_FNAME_LOG_FILE}}: '$(cat ${{env.BASE_FNAME_LOG_FILE}})'"
          echo "llama-cli: ${{env.BASE_FNAME_RESPONSE_FILE}}: '$(cat ${{env.BASE_FNAME_RESPONSE_FILE}})'"
          echo "⚠️  [WARNING] Continuing workflow despite llama-cli test failure"
        else
          echo "✅ [SUCCESS] llama-cli succeeded. Output (stdout):"
          echo "llama-cli: ${{env.BASE_FNAME_LOG_FILE}}: '$(cat ${{env.BASE_FNAME_LOG_FILE}})'"
          echo "llama-cli: ${{env.BASE_FNAME_RESPONSE_FILE}}: '$(cat ${{env.BASE_FNAME_RESPONSE_FILE}})'"
          echo "llama-cli: ${{env.BASE_FNAME_RESPONSE_FILE}} ($(wc -w ${{env.BASE_FNAME_RESPONSE_FILE}} | awk '{print $1}') words):\n'$(cat ${{env.BASE_FNAME_RESPONSE_FILE}})'"
        fi

    - name: test-llama-cli-response-with-regex-word-match
      if: env.TEST_LLAMA_CLI == 'true'
      run: |
        python_output=$(python ./scripts/test_regex_match_file_2.py "${{env.BASE_FNAME_RESPONSE_FILE}}" "${{env.LLAMA_RESPONSE_WORDS}}")
        echo "Full Python Output: $python_output"

        # Extract the last line of the output, which contains "True" or "False"
        return_value=$(echo "$python_output" | tail -n 1)

        # Use the extracted "boolean" string in a conditional
        # Check for both "True" and "true" for compatibility
        if [[ "$return_value" == "True" ]] || [[ "$return_value" == "true" ]]; then
          echo "✅ [SUCCESS]: $python_output"
        else
          echo "❌ [FAILURE]: $python_output"
        fi

    # ========================= llama-completion test (b8742+) ======================================

    - name: test-llama-completion
      if: env.TEST_LLAMA_COMPLETION == 'true'
      timeout-minutes: 45
      continue-on-error: true
      run: |
        echo "invoking llama-completion from build tag: ${{ inputs.llamacpp_build_tag }}"
        echo "env.LOCAL_FNAME_QUANTIZED_GGUF=${{env.LOCAL_FNAME_QUANTIZED_GGUF}}"
        echo "VERBOSE mode: ${{env.VERBOSE}}"

        VERBOSE_FLAG=""
        if [[ "${{env.VERBOSE}}" == "true" ]]; then
          VERBOSE_FLAG="--verbose"
        fi

        # llama-completion uses combined prompt (no separate system prompt)
        COMBINED_PROMPT="${{env.LLAMA_SYSTEM_PROMPT}} ${{env.LLAMA_REQUEST_PROMPT}}"

        ./$LLAMA_BIN_DIR/llama-completion \
          -m ./${{env.LOCAL_FNAME_QUANTIZED_GGUF}} \
          -p "$COMBINED_PROMPT" \
          -n ${{env.LLAMA_REQUEST_N_PREDICT}} \
          --temp ${{env.LLAMA_CLI_TEMP}} \
          $VERBOSE_FLAG \
          -no-cnv \
          --no-display-prompt \
          --log-file ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.log.txt 1>${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt

        EXIT_CODE=$?
        if [ $EXIT_CODE -ne 0 ]; then
          echo "❌ [ERROR] llama-completion failed with exit code $EXIT_CODE:"
          ls -al
          echo "llama-completion: log: '$(cat ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.log.txt)'"
          echo "llama-completion: response: '$(cat ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt)'"
          echo "⚠️  [WARNING] Continuing workflow despite llama-completion test failure"
        else
          echo "✅ [SUCCESS] llama-completion succeeded. Output (stdout):"
          echo "llama-completion: response ($(wc -w ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt | awk '{print $1}') words):\n'$(cat ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt)'"
        fi

    - name: show-llama-completion-response
      if: env.TEST_LLAMA_COMPLETION == 'true'
      run: |
        echo "=== llama-completion Response Output ==="
        cat ${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt

    - name: test-llama-completion-response-with-regex-word-match
      if: env.TEST_LLAMA_COMPLETION == 'true'
      run: |
        # Run the Python script and capture output
        if python ./scripts/test_regex_match_file_2.py "${{env.REPO_NAME}}-${{inputs.quantization}}-llama-completion.response.txt" "${{env.LLAMA_RESPONSE_WORDS}}"; then
          echo "✅ [SUCCESS]: Required words found in llama-completion response"
        else
          echo "❌ [FAILURE]: Required words not found in llama-completion response"
        fi

    # ========================= llama-cli response validation ======================================

    - name: show-response
      if: env.TEST_LLAMA_CLI == 'true'
      run: |
        cat ${{env.BASE_FNAME_RESPONSE_FILE}}

    # - name: test-llama-cli-response-with-regex
    #   if: env.TEST_LLAMA_CLI == 'true'
    #   run: |
    #     matched=$(python ./scripts/test_regex_match_file.py "${{env.LLAMA_RESPONSE_REGEX}}" ${{env.BASE_FNAME_RESPONSE_FILE}})
    #     echo "matched: '$matched'"
    #     if [[ "$matched" == "False" ]]; then
    #       echo "[FAILURE] Regex \`${{env.LLAMA_RESPONSE_REGEX}}\` NOT matched in llama-cli response."
    #       exit 1
    #     else
    #       echo "[SUCCESS] Regex \`${{env.LLAMA_RESPONSE_REGEX}}\` matched in llama-cli response."
    #     fi

    # - name: test-print-llama-cli-generated-log
    #   if: env.TEST_LLAMA_CLI == 'true' && env.DEBUG == 'true'
    #   continue-on-error: true
    #   run: |
    #     ls -al
    #     echo "llama-cli: ${{env.BASE_FNAME_LOG_FILE}}: '$(cat ${{env.BASE_FNAME_LOG_FILE}})'"

    # - name: test-print-llama-cli-response-with-metrics
    #   if: env.TEST_LLAMA_CLI == 'true'
    #   run: |
    #     echo "llama-cli: ${{env.BASE_FNAME_RESPONSE_FILE}} ($(wc -w ${{env.BASE_FNAME_RESPONSE_FILE}} | awk '{print $1}') words):\n'$(cat ${{env.BASE_FNAME_RESPONSE_FILE}})'"

    # ========================= Upload log as artifact ======================================

    - name: Upload log artifact
      if: ${{  env.TEST_LLAMA_CLI == 'true' || env.TEST_LLAMA_SERVER == 'true' || env.TEST_LLAMA_RUN == 'true' }}
      id: artifact-upload-log
      uses: actions/upload-artifact@v6
      with:
        overwrite: true
        retention-days: ${{env.GH_ARTIFACT_RETENTION}}
        name: ${{env.REPO_NAME}}-${{inputs.quantization}}-${{env.LLAMA_CLI_TOOL}}-log
        path: ${{env.BASE_FNAME_LOG_FILE}}

    - name: Show log artifact ID
      if: ${{  env.TEST_LLAMA_CLI == 'true' || env.TEST_LLAMA_SERVER == 'true' || env.TEST_LLAMA_RUN == 'true' }}
      id: show-artifact-id-log
      run:  echo 'Artifact ID is ${{ steps.artifact-upload-log.outputs.artifact-id }}'

    # ========================= Upload response as artifact ======================================

    - name: Upload response artifact
      if: ${{  env.TEST_LLAMA_CLI == 'true' || env.TEST_LLAMA_SERVER == 'true' || env.TEST_LLAMA_RUN == 'true' }}
      id: artifact-upload-response
      uses: actions/upload-artifact@v6
      with:
        overwrite: true
        retention-days: ${{env.GH_ARTIFACT_RETENTION}}
        name: ${{env.REPO_NAME}}-${{inputs.quantization}}-${{env.LLAMA_CLI_TOOL}}-response
        path: ${{env.BASE_FNAME_RESPONSE_FILE}}

    - name: Show response artifact ID
      if: ${{  env.TEST_LLAMA_CLI == 'true' || env.TEST_LLAMA_SERVER == 'true' || env.TEST_LLAMA_RUN == 'true' }}
      id: show-artifact-id-response
      run:  echo 'Artifact ID is ${{ steps.artifact-upload-response.outputs.artifact-id }}'

    # ========================= Upload log to release ======================================

    # - name: Get release info
    #   id: get-release-info
    #   run: |
    #     RELEASE_INFO=$(curl -sL \
    #       -H "Accept: application/vnd.github+json" \
    #       -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
    #       "https://api.github.com/repos/${{ github.repository}}/releases/tags/${{ github.ref_name }}")
    #     echo "RELEASE_INFO: $RELEASE_INFO"
    #     # TEST_UPLOAD_URL=$("$RELEASE_INFO" | jq -r '.upload_url')
    #     # echo "test_upload_url=$TEST_UPLOAD_URL" >> $GITHUB_ENV

    # - name: Get release upload URL
    #   id: get-release-step
    #   run: |
    #     UPLOAD_URL=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
    #       "https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ github.ref_name }}" \
    #       | jq -r '.upload_url')
    #     echo "upload_url=$UPLOAD_URL" >> $GITHUB_OUTPUT

    # - name: Upload llama.cpp CLI log to release assets
    #   id: upload-asset-llama-cli-log
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.get-release-step.outputs.upload_url }}
    #     asset_path: ${{env.BASE_FNAME_LOG_FILE}}
    #     asset_name: ${{env.BASE_FNAME_LOG_FILE}}
    #     asset_content_type: text/plain

    # ========================= Upload response to release ======================================

    # - name: Check for response file existence
    #   id: check_response_file
    #   run: |
    #     if [ -f "${{env.BASE_FNAME_RESPONSE_FILE}}" ]; then
    #       echo "response_file_exists=true"
    #       echo "response_file_exists=true" >> $GITHUB_OUTPUT
    #     else
    #       echo "response_file_exists=false"
    #       echo "response_file_exists=false" >> $GITHUB_OUTPUT
    #     fi

    # - name: Upload llama.cpp CLI response to release assets
    #   id: upload-asset-llama-cli-response
    #   if: steps.check_response_file.outputs.response_file_exists == 'true'
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.get-release-step.outputs.upload_url }}
    #     asset_path: ${{env.BASE_FNAME_RESPONSE_FILE}}
    #     asset_name: ${{env.BASE_FNAME_RESPONSE_FILE}}
    #     asset_content_type: text/plain

    # ========================= llama.cpp llama-server tests ======================================

    # - name: test-llama-server-with-quantized-gguf
    #   if: env.TEST_LLAMA_SERVER == 'true'
    #   timeout-minutes: 10
    #   run: |
    #     echo "--------------------"
    #     echo "LOCAL_FNAME_QUANTIZED_GGUF='$LOCAL_FNAME_QUANTIZED_GGUF' (${{ env.LOCAL_FNAME_QUANTIZED_GGUF }})"
    #     echo "--------------------"
    #     echo "Starting llama-server on port ${{env.LLAMA_SERVER_PORT}}"
    #     nohup ./bin/llama-server -m ./${{env.LOCAL_FNAME_QUANTIZED_GGUF}} --port ${{env.LLAMA_SERVER_PORT}} &

    # echo waiting for llama-server to start...
    # retry_count=0
    # max_retries=10
    # while ! nc -v -z localhost ${{env.LLAMA_SERVER_PORT}} && [ $retry_count -lt $max_retries ]; do
    #   echo "retry_count: $retry_count"
    #   if [ $? -eq 0 ]; then
    #     echo "Connection successful!"
    #     break
    #   else
    #     echo "Connection failed, retrying..."
    #     echo "sleeping 1 second..." && sleep 1
    #     retry_count=$((retry_count + 1))
    #   fi
    # done

    # - name: test-llama-server-connection
    #   if: env.TEST_LLAMA_SERVER == 'true'
    #   run: |
    #     echo "testing llama-server connection: ${{env.LLAMA_SERVER_HOST}}:${{env.LLAMA_SERVER_PORT}}..."
    #     echo "netstat:\n$(netstat -an -p tcp | grep ".${{env.LLAMA_SERVER_HOST}}")"

    #     if nc -v -z ${{env.LLAMA_SERVER_HOST}} ${{env.LLAMA_SERVER_PORT}}; then
    #       echo "SUCCESS: Connection to llama-server successful."
    #     else
    #       echo "Failure: Connection to llama-server failed."
    #       # ps aux
    #       exit 1
    #     fi

    # - name: test-llama-server-with-curl
    #   timeout-minutes: 5
    #   if: env.TEST_LLAMA_SERVER == 'true'
    #   run: |
    #     curl --request GET --url http://${{env.LLAMA_SERVER_HOST}}:${{env.LLAMA_SERVER_PORT}}/health
    #     echo "TODO: Test for {"status":"ok"} from GET /health"
    #     echo "${{env.LLAMA_REQUEST_PROMPT}}"
    #     curl --request POST \
    #       --url ttp://${{env.LLAMA_SERVER_HOST}}:${{env.LLAMA_SERVER_PORT}}/completion \
    #       --header "Content-Type: application/json" \
    #       --data '{ \
    #         "prompt": "${{env.LLAMA_REQUEST_PROMPT}}", \
    #         "n_predict": ${{env.LLAMA_REQUEST_N_PREDICT}}, \
    #         "seed": ${{env.LLAMA_REQUEST_SEED}} \
    #       }'
    #     echo "TODO: Test for JSON response from POST /completion endpoint using jq"

    # TODO:
    # - name: kill-llama-server
    #   if: env.TEST_LLAMA_SERVER == 'true'
    #   run: |
    #     echo "TODO: is netstat and ps commands to get PID and kill..."

    # ========================= llama.cpp llama-run tests ======================================

    # - name: test-llama-run
    #   if: env.TEST_LLAMA_RUN == 'true'
    #   timeout-minutes: 10
    #   continue-on-error: true
    #   run: |
    #     echo "invoking llama-run to test inference..."
    #     ${{env.LLAMA_RUN_BIN}} ./${{env.LOCAL_FNAME_QUANTIZED_GGUF}} -p ${{env.LLAMA_REQUEST_PROMPT}} ${{env.LLAMA_RUN_FLAGS}}
    #     if [ $? -eq 1 ]; then
    #       exit 1
    #     fi

